cd ~/PycharmProjects/tce-reggie/crm/shop
vagrant up --provider=virtualbox


vagrant ssh

# Внутри VM:
cd /opt/event-system

# Скопируйте файлы (если install.sh не сделал этого)
sudo cp -r /vagrant/docker/* .
sudo cp /vagrant/requirements.txt .
sudo cp /vagrant/docker/.env.example .env

# Запустите
sudo docker-compose up -d --build

# Проверка контейнеров
sudo docker-compose ps

# Если на предыдущих этапах возникают ошибки,
# то попобовать эту командку:
sudo docker-compose logs web --tail=50

# Если выведет, что проблемы с enterpoint.sh, то:
dos2unix /vagrant/docker/entrypoint.sh

для отключения и очистки содержимого вм :
exit
cd ~/PycharmProjects/tce-reggie/crm/shop

# Остановите VM
vagrant halt

# Удалите VM полностью
vagrant destroy -f

# Удалите служебную папку Vagrant
rm -rf .vagrant